<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
  <style name="Base.Theme.ELibrary" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/bluegrey800</item>
        <item name="colorSecondary">@color/grey600</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

  <!-- Inherit from Base Theme -->
  <style name="Theme.ELibrary" parent="Base.Theme.ELibrary" />
</resources>
